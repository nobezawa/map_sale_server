<%= form_for(@shop_category) do |f| %>
  <% if @shop_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_category.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% @shop_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
    カテゴリー名<br>
    <%= f.text_field :type_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
